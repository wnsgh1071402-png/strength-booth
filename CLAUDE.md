# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

청소년 웰니스 부스에서 운영하는 VIA 강점검사 웹앱 "빛나는 나 발견하기". 참여자는 **각자 폰**으로 검사하고, 결과는 **상담자 태블릿**에 실시간으로 뜬다. 두 기기가 다르다는 것이 이 앱의 모든 설계를 결정한다.

빌드 단계가 없다. 정적 파일을 GitHub Pages가 그대로 서빙하고, Firebase는 CDN ESM으로 런타임에 불러온다. `npm install` 할 것이 없고 `package.json`도 없다.

## 명령어

```bash
# 로컬 확인 — ES 모듈이라 file:// 로 열면 동작하지 않는다
python -m http.server 8765          # http://127.0.0.1:8765/index.html

# 콘텐츠 정합성 + 채점 검증 (data.js를 고쳤으면 반드시)
node verify.mjs

# 배포 — main에 push하면 GitHub Pages가 자동 반영 (30초~2분)
git push origin main
```

`verify.mjs`는 영역 4 / 강점 24 / 문항 96 / 상담가이드 24세트, 문구·VIA근거 중복 없음, 셔플 재현성, 채점 경계값(전부 5점→20점, 전부 1점→4점), 동점 처리를 검사한다.

### 브라우저 E2E

Playwright로 실제 검증이 가능하지만 **번들 chromium 다운로드가 이 환경에서 실패**한다. 시스템 Chrome을 쓸 것:

```js
chromium.launch({ channel: 'chrome' })   // playwright-core, 스크래치패드에 npm i
```

폰→태블릿 동기화를 검증할 때는 **반드시 `browser.newContext()`를 두 개 만들어** 저장소를 분리해야 한다. 같은 컨텍스트를 쓰면 localStorage 폴백이 성공을 흉내내서 Firestore를 거치지 않아도 테스트가 통과한다.

## 배포

| 대상 | 주소 |
|---|---|
| 참여자 (QR 배포) | https://wnsgh1071402-png.github.io/strength-booth/ |
| 상담자 (북마크) | `/staff.html` · PIN은 `assets/firebase-config.js`의 `STAFF_PIN` |
| QR 인쇄 | `/qr.html` |

저장소: `wnsgh1071402-png/strength-booth` (public). Firebase 프로젝트: `light-7a28b`.

## 아키텍처

`index.html`(참여자)과 `staff.html`(상담자)이 `assets/`를 공유하는 두 진입점. 화면 코드는 `store.js` 뒤에서 클라우드/로컬 모드를 구분하지 않는다.

### store.js — 이중 모드와 그 함정

`firebase-config.js`가 비어 있거나 SDK 초기화가 실패하면 자동으로 **로컬 모드**(localStorage)로 떨어진다. 덕분에 Firebase 없이도 전체 개발·테스트가 가능하고, 부스 와이파이가 끊겨도 검사는 굴러간다.

**여기에 실제로 사고를 냈던 함정이 있다.** Firestore의 `addDoc`은 서버에 닿지 못해도 로컬 큐에 넣고 resolve된다. DB가 없던 시절 폰은 "전송 성공", 태블릿은 "실시간 연결됨"을 표시했지만 아무것도 넘어가지 않았다. 그래서:

- 제출은 `addDoc` 뒤에 **`waitForPendingWrites`로 서버 수신을 확인**한 뒤에만 성공으로 본다 (8초 타임아웃)
- 상담자 배지는 **실제 서버 스냅샷**(`snapshot.metadata.fromCache === false`)을 받은 뒤에만 초록이 된다
- DB가 없으면 `onSnapshot`은 오류조차 내지 않고 무한 재시도만 하므로, `staff.js`에 10초 워치독을 둬서 원인을 표시한다

동기화 상태를 다룰 때 이 확인 단계를 우회하지 말 것. 거짓 성공은 부스에서 뒤늦게 발견된다.

### 세션은 날짜로 자동 분리

Firestore 경로는 `booths/{YYYY-MM-DD}/results/{autoId}`이고, 날짜는 클라이언트가 로컬 시각으로 계산한다. QR URL에 세션 파라미터를 넣지 않은 이유가 이것이다 — **QR을 한 번만 인쇄하면 며칠을 운영해도 집계가 날짜별로 갈린다.** "세션 초기화"는 데이터 삭제가 아니라 상담자 화면에서 날짜를 바꾸는 것이다(보안 규칙이 delete를 막고 있으므로).

### scoring.js — 고정 시드 셔플

같은 강점 4문항이 연달아 나오면 응답 편향이 생기므로 섞되, **영역 id를 시드로 한 결정론적 셔플**이라 누가 언제 열어도 순서가 같다. 답안 배열(24개 숫자)만으로 채점을 재현할 수 있는 근거가 이것이다.

> `data.js`의 문항을 추가·삭제·재배치하면 출제 순서가 바뀌어 **이전 답안 배열로는 채점을 재현할 수 없다.** 부스 운영 중에는 콘텐츠를 고치지 말 것.

### data.js — 콘텐츠 단일 출처

문항·결과문구·살리는법·상담가이드가 전부 여기 있다. VIA 24개 강점을 **4영역 × 6개로 중복·누락 없이** 배분했다(`verify.mjs`가 강제). 원래 기획 문서의 '집중·회복탄력성·개방성·자기이해'는 VIA 24개에 없어서 각각 끈기·자기조절/낙관성·판단력·통찰로 흡수했다.

`interpret`는 상담자 화면 전용이며 참여자에게는 보이지 않는다. `pairs`는 **다른 영역의 강점 id를 가리킬 수 있어서** `findStrength(id)`로 전역 조회한다(`getStrength`는 영역 내부만 찾는다).

### 상담자 화면의 두 가지 원천

한때 "남을 돌보느라 자기 감정을 뒤로 미루는 경우가 많습니다", "자존감을 꼭 확인해보세요" 같은 문장이 있었으나 전부 걷어냈다. **검사는 강점 점수만 쟀고 성격·배경·속마음은 잰 적이 없다.**

지금 화면에 나오는 것은 출처가 둘 중 하나로 명확하다:

**1. 학생의 응답에서 나오는 것**
- 문항별 응답 그대로 (`answers` → `explainAnswers`)
- 점수 분포에서만 읽히는 것 (`readPattern` — 1위·6위 차, 평균, 고른 영역)
- 한 강점 안의 응답 편차 (`innerPattern` — 합산 점수에서는 사라지는 정보)

**2. VIA 원척도 문헌에서 나오는 것** (`interpret`)
- 6덕목 분류와 구성개념 정의 — Peterson & Seligman(2004), VIA Institute
- 과잉·과소·최적 사용 — Niemiec(2019) *Finding the golden mean*
- 함께 나타나는 강점 — McGrath(2013) VIA 데이터베이스 45.8만 명 상관

문헌은 **참고해서 한국어로 새로 썼다.** Niemiec의 표를 그대로 옮기지 않는다(VIA 저작물). 이 프로젝트의 저작권 원칙과 같다.

**이 화면에 문장을 추가할 때는 위 두 원천 중 어디서 나온 것인지 말할 수 있어야 한다.** 아니면 넣지 말 것.

> ⚠️ 이 검사는 VIA-IS 원척도가 아니라 부스용으로 새로 만든 도구라 **규준(norm)이 없다.** 백분위·또래 비교·절단점 같은 규준 기반 해석을 절대 추가하지 말 것. 점수는 6개 중 순위로만 다룬다. 이 고지는 `staff.html`의 `.use-note`에 있다.

## 지켜야 할 것

**개인정보를 수집하지 않는다.** 이름·나이·학교를 묻지 않고, 식별은 당일용 4자리 임시 번호뿐이다. Firestore 규칙이 읽기를 공개하고 있으므로 이 원칙이 곧 프라이버시 보호다. 개인 식별 필드를 추가하지 말 것.

**말투** — 참여자 화면은 **합니다체**. 단, 두 가지 예외가 의도적이다:
- 검사 문항 96개는 `~한다` 평서형 유지 (자기보고식 검사 문항의 표준 형식)
- 상담 가이드 안의 반말 인용(`"네가 없었으면 그 팀이 어땠을 것 같아?"`)은 상담자가 학생에게 건네는 예시 질문이라 그대로 둔다

**STAFF_PIN은 보안 장치가 아니다.** 저장소가 public이라 소스에서 읽힌다. 참여자 오진입 방지용이다.

**Firestore 보안 규칙** — 읽기·생성은 공개, 수정·삭제는 차단. 공개 write이므로 장난 데이터는 원리상 막을 수 없지만 기존 결과 훼손은 막는다. 규칙 원문은 [README.md](README.md)에 있다.

**외부 라이브러리를 늘리지 않는다.** 결과 카드 PNG는 canvas로 직접 그리고(`participant.js`의 `drawResultImage`), 유일한 CDN 의존은 Firebase SDK와 `qr.html`의 QR 생성기다.

## 참고 문서

- [README.md](README.md) — Firebase 설정 절차, 보안 규칙 원문, 부스 운영 흐름
- [프로젝트_인수인계_1.md](프로젝트_인수인계_1.md), [관계에서의_나_강점검사.md](관계에서의_나_강점검사.md) — 원래 기획 문서. 앱과 어긋나는 부분이 있다(집계 방식, 말투). 앱 쪽이 최신이다.
